# @defaultRequired=true
# @defaultSensitive=true
# @currentEnv=$NODE_ENV
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# ---

# @public @type=enum(development, production, test)
NODE_ENV=development

# The API server (apps/server). Dispatch signs in with DISPATCH_ADMIN_EMAIL /
# DISPATCH_ADMIN_PASSWORD set on the server, and the server must list this
# app's origin in DISPATCH_ORIGIN for CORS.
# @public @type=string(matches="^(https?://|/(?!/))")
NEXT_PUBLIC_SERVER_URL=
